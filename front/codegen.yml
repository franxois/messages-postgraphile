overwrite: true
schema: "http://localhost:32100/graphql"
documents: "src/**/*.{tsx,graphql}"
generates:
  src/generated/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-resolvers"
      - "typescript-react-apollo"

